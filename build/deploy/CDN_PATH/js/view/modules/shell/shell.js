define([
    "jquery",
    "underscore",
    "backbone",
    "config",
    "router",
    "handlebars",
    "util/utils",
    "model/app_model",
    "view/common/base_view"
], function (
    $,
    _,
    Backbone,
    Config,
    Router,
    Handlebars,
    Utils,
    AppModel,
    BaseView
) {

    "use strict";

    return BaseView.extend({

        initialize: function() {
           
        },

        render: function() {}
    });
});